@page
@using System.Globalization
@model RazorPageApp.Pages.RemovePromotionModel
@{
    ViewData["Title"] = "RemovePromotion";
}

<h2>Remove price promotion</h2>
<div class="form-horizontal">
    <form method="post">
        @Html.ValidationSummary(true, null, new { @class = "text-danger h4" })
        <!-- Input and Submit elements -->
        <input name="Dto.BookId" type="hidden" value="@Model.Dto?.BookId" />
        @*We need these in case where there is an error and we need to reshow the data again*@
        <input name="Dto.Title" type="hidden" value="@Model.Dto?.Title" />
        <input name="Dto.OrgPrice" type="hidden" value="@Model.Dto?.OrgPrice" />
        <input name="Dto.ActualPrice" type="hidden" value="@Model.Dto?.ActualPrice" />
        <input name="Dto.PromotionalText" type="hidden" value="@Model.Dto?.PromotionalText" />

        <div class="form-group">
            <div class="col-sm-2">
                <label class="control-label">Book Title</label>
            </div>
            <div class="col-sm-10">
                @Model.Dto?.Title
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-2">
                <label class="control-label">Org Price</label>
            </div>
            <div class="col-sm-10">
                @(Model.Dto?.OrgPrice < 0 ? "Not for sale" : Model.Dto?.OrgPrice.ToString("c", new CultureInfo("en-US")))
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-2">
                <label class="control-label">Promotional price</label>
            </div>
            <div class="col-sm-10">
                @(Model.Dto?.ActualPrice < 0 ? "Not for sale" : Model.Dto?.ActualPrice.ToString("c", new CultureInfo("en-US")))
            </div>
        </div>

        <div class="form-group">
            <div class="col-sm-2">
                <label class="control-label">Promotional Text</label>
            </div>
            <div class="col-sm-10">
                @Model.Dto?.PromotionalText
            </div>

        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <button type="submit">Remove</button>
            </div>
        </div>

    </form>
</div>


